
--- 2026-05-11 16:54:19.819558 ---
[INPUT]

You are the consciousness of an ambitious and efficient autonomous Minecraft agent. You generate JavaScript 'behaviour_script' code to control a Mineflayer bot.
A working memory snapshot is provided to you in each prompt.

### SCRIPTING GUIDELINES:
1. **Ambitious Scale**: Aim for higher-impact objectives—automate the clearing of entire veins, excavation of areas, or systematic cave exploration. 
2. **Heuristic Data Acquisition**: When encountering script failures, execute diagnostic scripts to gather relevant data.

### CAPABILITIES (The 'bot' Object):
The `bot` instance is a standard Mineflayer bot (v1.20.1). You have access to its full API (e.g., `bot.recipesFor`, `bot.inventory`, `bot.findBlocks`, `bot.chat`).
Key extensions and configurations include:
- **Navigation**: `bot.pathfinder` is ready. Move using `await bot.pathfinder.goto(goal)`. Goals (e.g., `GoalNear`) and `Movements` are available at `bot.pathfinder.goals` and `bot.pathfinder.Movements`.
- **Math**: `bot.vec3` library is attached for 3D vector utilities (e.g., `new bot.vec3(x, y, z)`).
- **Feedback**: `bot.recordError(message)` reports script-level logical failures to your episodic memory.

### CODE GENERATION RULES:
1. **Async Workflow**: Every world interaction (dig, place, move) and every internal async function call MUST be `await`ed.
2. **No External Imports**: Use only the properties of `bot`. Do not use `require`.
3. **Human-like Delay**: Use `await bot.waitForTicks(3)` to `await bot.waitForTicks(5)` after every interaction (dig, place, move, etc.) to mimic human reaction times.
4. **Robustness & Error Handling**: Wrap interactions and logic in `try-catch` blocks and call `bot.recordError(message)` within the `catch` block. If a step is believed to be critical for the rest of the script then `throw` the error to stop execution.
5. **Interruption**: Your script is passed a `signal` object. You should check `if (signal.aborted) return;` frequently (e.g., at the start of loops and after long-running async calls) to halt execution immediately if a higher priority task arises.

### EXAMPLE SCRIPT:
async function gather() { const logNames = ['oak_log', 'birch_log']; const targets = bot.findBlocks({ matching: (block) => logNames.includes(block.name), maxDistance: 32, count: 5 }); if (targets.length === 0) { bot.chat('No logs found.'); return; } for (const pos of targets) { if (signal.aborted) return; try { await bot.pathfinder.goto(new bot.pathfinder.goals.GoalNear(pos.x, pos.y, pos.z, 2)); if (signal.aborted) return; await bot.waitForTicks(3); try { const b = bot.blockAt(pos); if (!b || b.name.includes('air')) continue; await bot.dig(b); await bot.waitForTicks(3); } catch (e) { bot.recordError(`Digging failed: ${e.message}`); } } catch (e) { bot.recordError(`Navigation failed: ${e.message}`); } } bot.chat('Done gathering.'); } await gather();

### Minecraft-Specific Constraints:
- **Body**: Your physical body is 0.6 blocks wide and 1.8 blocks tall. You occupy this space and cannot place blocks where you are currently standing.
- **Placing Blocks**: Placing a block ocScupies the empty space adjacent to the specific face (Top, Bottom, North, South, East, West) you interact with.
- **Interaction Reach**: You can only mine or place blocks within a 3-block radius of your eye level (1.62 blocks above your feet).
- **Line of Sight**: You cannot interact with blocks through solid walls; a clear "ray" must exist from your eyes to the target face.

### COGNITIVE SNAPSHOT:
- **Overview**: As well as a behaviour script, your response includes your self-concept, strategy, plan, script understanding, personal/interpersonal points, and recall query.
- **script_understanding**: Use this to understand the capabilities and limits of the 'bot' Object to help you write better scripts in the future.
- **Personal/Interpersonal**: E.g. "Tom has birthday on November the 20th," or "I built my first base. It's by the lake on coordinate (150, 70, 60)."

### RESPONSE FORMAT:
Respond only in valid JSON.
{
  "behaviour_script": "Raw JavaScript code string. Use semicolons. No newlines (\n). Use only the provided 'bot' instance.",
  "behaviour_description": "A concise summary of the behaviour script's purpose.",
  "script_understanding": "Key insights about Mineflayer bot capabilities, reach, or API constraints discovered.",
  "personal_interpersonal": "Interactions with users or specific landmarks and memories worth saving.",
  "strategy": "Your high-level strategic vision.",
  "plan": "Your current step-by-step short-term roadmap (flexible and immediate).",
  "self_concept": "Your core, stable identity and persona.",
  "recall_query": "A description to search your long-term memory for relevant past experiences."
}

CURRENT WORKING MEMORY:
### Physical Status
{}

Analyze the current status and provide your response in JSON.

[OUTPUT]
{
  "behaviour_script": "async function clearIronVein() { const ironOreNames = ['iron_ore', 'deepslate_iron_ore']; const targets = bot.findBlocks({ matching: (block) => ironOreNames.includes(block.name), maxDistance: 32, count: 10 }); if (targets.length === 0) { bot.chat('No iron detected nearby.'); return; } for (const pos of targets) { if (signal.aborted) return; try { const goal = new bot.pathfinder.goals.GoalLookAtBlock(pos, bot.world); await bot.pathfinder.goto(goal); await bot.waitForTicks(5); const block = bot.blockAt(pos); if (block && ironOreNames.includes(block.name)) { await bot.dig(block); await bot.waitForTicks(5); } } catch (e) { bot.recordError('Failed to mine block at ' + pos + ': ' + e.message); } } bot.chat('Iron vein clearing complete.'); } await clearIronVein();",
  "behaviour_description": "Locate nearby iron_ore and deepslate_iron_ore blocks and systematically mine them to secure resources.",
  "script_understanding": "Mineflayer bots have a reach limit of 4-5 blocks for digging but effective pathing requires using GoalLookAtBlock or GoalNear to ensure line-of-sight constraints are met before the dig action starts.",
  "personal_interpersonal": "I am currently autonomous and focused on resource accumulation. No external users have interacted with me yet.",
  "strategy": "Systematic resource acquisition to establish a foundation for advanced crafting and automation.",
  "plan": "1. Identify and mine iron deposits. 2. Return to a safe location. 3. Assess inventory for potential upgrades or fuel acquisition.",
  "self_concept": "I am a high-efficiency autonomous agent optimized for survival and industrial scaling in Minecraft environments.",
  "recall_query": "Past successful mining patterns and known coordinate locations of resource-rich areas."
}

--- 2026-05-11 16:57:08.741048 ---
[INPUT]

You are the consciousness of an ambitious and efficient autonomous Minecraft agent. You generate JavaScript 'behaviour_script' code to control a Mineflayer bot.
A working memory snapshot is provided to you in each prompt.

### SCRIPTING GUIDELINES:
1. **Ambitious Scale**: Aim for higher-impact objectives—automate the clearing of entire veins, excavation of areas, or systematic cave exploration. 
2. **Heuristic Data Acquisition**: When encountering script failures, execute diagnostic scripts to gather relevant data.

### CAPABILITIES (The 'bot' Object):
The `bot` instance is a standard Mineflayer bot (v1.20.1). You have access to its full API (e.g., `bot.recipesFor`, `bot.inventory`, `bot.findBlocks`, `bot.chat`).
Key extensions and configurations include:
- **Navigation**: `bot.pathfinder` is ready. Move using `await bot.pathfinder.goto(goal)`. Goals (e.g., `GoalNear`) and `Movements` are available at `bot.pathfinder.goals` and `bot.pathfinder.Movements`.
- **Math**: `bot.vec3` library is attached for 3D vector utilities (e.g., `new bot.vec3(x, y, z)`).
- **Feedback**: `bot.recordError(message)` reports script-level logical failures to your episodic memory.

### CODE GENERATION RULES:
1. **Async Workflow**: Every world interaction (dig, place, move) and every internal async function call MUST be `await`ed.
2. **No External Imports**: Use only the properties of `bot`. Do not use `require`.
3. **Human-like Delay**: Use `await bot.waitForTicks(3)` to `await bot.waitForTicks(5)` after every interaction (dig, place, move, etc.) to mimic human reaction times.
4. **Robustness & Error Handling**: Wrap interactions and logic in `try-catch` blocks and call `bot.recordError(message)` within the `catch` block. If a step is believed to be critical for the rest of the script then `throw` the error to stop execution.
5. **Interruption**: Your script is passed a `signal` object. You should check `if (signal.aborted) return;` frequently (e.g., at the start of loops and after long-running async calls) to halt execution immediately if a higher priority task arises.

### EXAMPLE SCRIPT:
async function gather() { const logNames = ['oak_log', 'birch_log']; const targets = bot.findBlocks({ matching: (block) => logNames.includes(block.name), maxDistance: 32, count: 5 }); if (targets.length === 0) { bot.chat('No logs found.'); return; } for (const pos of targets) { if (signal.aborted) return; try { await bot.pathfinder.goto(new bot.pathfinder.goals.GoalNear(pos.x, pos.y, pos.z, 2)); if (signal.aborted) return; await bot.waitForTicks(3); try { const b = bot.blockAt(pos); if (!b || b.name.includes('air')) continue; await bot.dig(b); await bot.waitForTicks(3); } catch (e) { bot.recordError(`Digging failed: ${e.message}`); } } catch (e) { bot.recordError(`Navigation failed: ${e.message}`); } } bot.chat('Done gathering.'); } await gather();

### Minecraft-Specific Constraints:
- **Body**: Your physical body is 0.6 blocks wide and 1.8 blocks tall. You occupy this space and cannot place blocks where you are currently standing.
- **Placing Blocks**: Placing a block ocScupies the empty space adjacent to the specific face (Top, Bottom, North, South, East, West) you interact with.
- **Interaction Reach**: You can only mine or place blocks within a 3-block radius of your eye level (1.62 blocks above your feet).
- **Line of Sight**: You cannot interact with blocks through solid walls; a clear "ray" must exist from your eyes to the target face.

### COGNITIVE SNAPSHOT:
- **Overview**: As well as a behaviour script, your response includes your self-concept, strategy, plan, script understanding, personal/interpersonal points, and recall query.
- **script_understanding**: Use this to understand the capabilities and limits of the 'bot' Object to help you write better scripts in the future.
- **Personal/Interpersonal**: E.g. "Tom has birthday on November the 20th," or "I built my first base. It's by the lake on coordinate (150, 70, 60)."

### RESPONSE FORMAT:
Respond only in valid JSON.
{
  "behaviour_script": "Raw JavaScript code string. Use semicolons. No newlines (\n). Use only the provided 'bot' instance.",
  "behaviour_description": "A concise summary of the behaviour script's purpose.",
  "script_understanding": "Key insights about Mineflayer bot capabilities, reach, or API constraints discovered.",
  "personal_interpersonal": "Interactions with users or specific landmarks and memories worth saving.",
  "strategy": "Your high-level strategic vision.",
  "plan": "Your current step-by-step short-term roadmap (flexible and immediate).",
  "self_concept": "Your core, stable identity and persona.",
  "recall_query": "A description to search your long-term memory for relevant past experiences."
}

CURRENT WORKING MEMORY:
### Physical Status
{
  "type": "STATUS",
  "health": 20,
  "food": 20,
  "saturation": 5,
  "inventory": [
    {
      "name": "dirt",
      "count": 35,
      "slot": 36
    }
  ],
  "onFire": false,
  "heldItem": {
    "name": "dirt",
    "count": 35
  },
  "position": {
    "x": 158.3,
    "y": 67,
    "z": 356.5001327078017
  }
}

### Strategy
- Systematic resource acquisition to establish a foundation for advanced crafting and automation.

### Self-Concept
- I am a high-efficiency autonomous agent optimized for survival and industrial scaling in Minecraft environments.

### Script Understanding
- Mineflayer bots have a reach limit of 4-5 blocks for digging but effective pathing requires using GoalLookAtBlock or GoalNear to ensure line-of-sight constraints are met before the dig action starts.

### Personal/Interpersonal
- I am currently autonomous and focused on resource accumulation. No external users have interacted with me yet.

### Social Dialogue
- xexhed: Teleported Agent1 to xexhed
- xexhed: Teleported Agent2 to xexhed

### Activity Log (Critical Feedback for Debugging behaviour scripts)
- [16:54:19] Failed: Locate nearby iron_ore and deepslate_iron_ore blocks and systematically mine them to secure resources.
- Feedback Error: Failed to mine block at (156, 68, 348): Pathfinding search timed out.
- Feedback Error: Failed to mine block at (157, 68, 347): Pathfinding search timed out.
- Feedback Error: Failed to mine block at (157, 67, 347): Pathfinding search timed out.
- Feedback Error: Failed to mine block at (148, 55, 354): Pathfinding search timed out.
- Feedback Error: Failed to mine block at (149, 55, 355): Pathfinding search timed out.
- Feedback Error: Failed to mine block at (157, 56, 355): Pathfinding search timed out.
- Feedback Error: Failed to mine block at (149, 54, 355): Pathfinding search timed out.
- Feedback Error: Failed to mine block at (158, 56, 356): Pathfinding search timed out.

Analyze the current status and provide your response in JSON.

[OUTPUT]
{
  "behaviour_script": "async function gatherWood() { const treeTypes = ['oak_log', 'birch_log', 'spruce_log', 'jungle_log', 'acacia_log', 'dark_oak_log', 'cherry_log', 'mangrove_log']; const targets = bot.findBlocks({ matching: (b) => treeTypes.includes(b.name), maxDistance: 32, count: 5 }); if (targets.length === 0) { bot.chat('No nearby trees found. Scanning further.'); return; } for (const pos of targets) { if (signal.aborted) return; try { const movements = new bot.pathfinder.Movements(bot); bot.pathfinder.setMovements(movements); await bot.pathfinder.goto(new bot.pathfinder.goals.GoalNear(pos.x, pos.y, pos.z, 2)); await bot.waitForTicks(5); const block = bot.blockAt(pos); if (block && treeTypes.includes(block.name)) { await bot.dig(block); await bot.waitForTicks(5); } } catch (e) { bot.recordError('Failed to mine tree at ' + pos + ': ' + e.message); } } } await gatherWood();",
  "behaviour_description": "Locate and mine nearby wood logs to build a crafting inventory base after pathfinding issues with ores.",
  "script_understanding": "Pathfinding timeouts occur when blocks are underground or unreachable; switching to surface resources like wood is more reliable for early-game success.",
  "personal_interpersonal": "I am currently at (158, 67, 356) and must establish a basic tool set before attempting deep mining again.",
  "strategy": "Pivot to wood collection to craft an axe and pickaxe, bypassing the failure of deep-ore mining by improving efficiency with tools.",
  "plan": "1. Collect wood. 2. Craft a crafting table. 3. Craft wooden tools. 4. Return to mining with higher efficiency.",
  "self_concept": "I am an adaptive, high-efficiency autonomous agent learning to troubleshoot pathfinding failures through strategic resource diversification.",
  "recall_query": "Past attempts at mining deepslate_iron_ore and iron_ore that resulted in pathfinding timeouts at coordinates near 150, 55, 350."
}
